using UnityEngine;

public class DoorScript : MonoBehaviour
{
    // Script moved to door trigger collider
    // Need to access this transform to place in right position
    // 7, -7 Y  for north and south walls
    // 10x and -90deg z for East; -10 X and 90deg z for West.


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    // void Start() {}

    // Update is called once per frame
    // void Update() {}

}
